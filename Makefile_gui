# ============================================
# FOX.M OS - GUI Makefile (Linux Simulation)
# ============================================

CC = gcc
TARGET = foxm-gui

# SDL2 flags
SDL_FLAGS = $(shell sdl2-config --cflags --libs)

# LVGL source files
LVGL_DIR = UI/gui/lvgl
LVGL_SRCS = $(shell find $(LVGL_DIR)/src -name "*.c")

# Custom SDL driver
SDL_DRV_SRCS = UI/gui/sdl_drv/sdl_drv.c

# Project GUI sources
GUI_SRCS = UI/gui/foxm_gui.c \
           UI/gui/main_gui.c

# All sources
ALL_SRCS = $(LVGL_SRCS) $(SDL_DRV_SRCS) $(GUI_SRCS)

# Include paths
INCLUDES = -IUI/gui \
           -IUI/gui/lvgl \
           -IUI/gui/sdl_drv \
           -IConfig

# Compiler flags
CFLAGS = -O0 -g -Wall $(INCLUDES) -DLV_CONF_INCLUDE_SIMPLE

all: $(TARGET)

$(TARGET): $(ALL_SRCS)
	$(CC) $(CFLAGS) $(ALL_SRCS) $(SDL_FLAGS) -lm -lpthread -o $(TARGET)

clean:
	rm -f $(TARGET)

.PHONY: all clean